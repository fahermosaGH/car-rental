security:
  password_hashers:
    App\Entity\User: 'auto'

  providers:
    app_user_provider:
      entity:
        class: App\Entity\User
        property: email

  firewalls:
    dev:
      pattern: ^/(?:_(profiler|wdt)|css|images|js)/
      security: false

    login:
      pattern: ^/api/login_check
      stateless: true
      json_login:
        check_path: /api/login_check
        username_path: email
        password_path: password
        success_handler: lexik_jwt_authentication.handler.authentication_success
        failure_handler: lexik_jwt_authentication.handler.authentication_failure

    api:
      pattern: ^/api
      stateless: true
      provider: app_user_provider
      jwt: ~

  access_control:
    # Públicos
    - { path: ^/api/login_check, roles: PUBLIC_ACCESS }
    - { path: ^/api/register, roles: PUBLIC_ACCESS }

    # GET públicos necesarios para la app sin login (CORS incluye OPTIONS)
    - { path: ^/api/locations, roles: PUBLIC_ACCESS, methods: [GET, OPTIONS] }
    - { path: ^/api/vehicles/available, roles: PUBLIC_ACCESS, methods: [GET, OPTIONS] }
    - { path: ^/api/vehicles, roles: PUBLIC_ACCESS, methods: [GET, OPTIONS] }
    - { path: ^/api/check-availability, roles: PUBLIC_ACCESS, methods: [GET, OPTIONS] }

    # Resto del API: requiere JWT
    - { path: ^/api, roles: IS_AUTHENTICATED_FULLY }
