# config/services.yaml

parameters: { }

services:
    # Config por defecto
    _defaults:
        autowire: true
        autoconfigure: true

    # 👇 REGISTRA TODOS LOS CONTROLADORES COMO CONTROLLER SERVICES
    App\Controller\:
        resource: '../src/Controller/'
        tags: ['controller.service_arguments']

    # 👇 Resto de servicios de la app
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # Si tenés servicios puntuales, van acá abajo
    # App\Algo\MiServicio:
    #     arguments: ['@otro_servicio']
