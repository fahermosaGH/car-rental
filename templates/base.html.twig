<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>{% block title %}Car Rental{% endblock %}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="{{ path('home') }}">CarRental</a>
    <div class="navbar-nav">
      <a class="nav-link" href="{{ path('quote') }}">Cotizar</a>
      <a class="nav-link" href="{{ path('reserve') }}">Reservar</a>
      <a class="nav-link" href="{{ path('my_reservations') }}">Mis reservas</a>
    </div>
  </div>
</nav>

<main class="container py-4">
  {% block body %}{% endblock %}
</main>

{# === Flatpickr CDN global === #}
<script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>

</body>
</html>
